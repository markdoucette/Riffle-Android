<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
              android:orientation="vertical"
              android:background="@drawable/fishon"
                tools:context=".MainActivity"
              android:weightSum="1">

    <!--android:paddingLeft="@dimen/activity_horizontal_margin"-->
    <!--android:paddingRight="@dimen/activity_horizontal_margin"-->
    <!--android:paddingTop="@dimen/activity_vertical_margin"-->
    <!--android:paddingBottom="@dimen/activity_vertical_margin"-->

    <Space
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="0.60"
            android:id="@+id/act_main_spacer"/>

    <Button
            android:layout_width="match_parent"
            android:layout_height="@dimen/act_main_btn_height"
            android:text="@string/add_trip_btn_label"
            android:id="@+id/act_main_btn_add_trip"
            android:layout_gravity="center_horizontal"
            />
    <Button
            android:layout_width="match_parent"
            android:layout_height="@dimen/act_main_btn_height"
            android:text="@string/view_trip_btn_label"
            android:id="@+id/act_main_btn_view_trip"
            android:layout_gravity="center_horizontal"
            />
</LinearLayout>
